pipeline {
    agent any

    stages any{
 
       stage('Git Checkout'){
 
        steps{
 
            script{
 
                git branch:'main',url:
                'https://github.com/Bussyoshodi7/Git-Checkout.git'
            }
        }
       }
    }
}
